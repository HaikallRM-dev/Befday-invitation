import React from 'react';
import TemplatePage from '../TemplatePage';
import config from '../../config/formal.json';

export default function FormalPage() {
  return <TemplatePage config={config} />;
}
